Cover art goes here as `<game-id>.png` (e.g. `powerstone.png`, `msr.png`).

Art is not shipped with the repository; without it the rail draws a letter
tile per game. The launcher looks here first, then for `icon.png` or any
`*.png` in the game directory itself.
